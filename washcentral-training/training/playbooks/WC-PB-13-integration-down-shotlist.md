# WC-PB-13 — Integration down: Zoho, SSO, and accounting · Snagit Shot List (operator-ready)

v1.0 · July 9, 2026 · Target output: **WC-PB-13-integration-down-v1.0.mp4** · MP4 (H.264) · 30 fps · **1920×1080 native, no scaling** · mic and system audio OFF (narration is ElevenLabs only)
Step numbers match the guide and the narration blocks one-to-one (12 steps).
**Verified against demo.washcentral.com on July 9, 2026.**

## Pre-roll checklist (not recorded)

- [ ] Display/capture region exactly 1920×1080; verify the first export's pixel dimensions before recording anything else.
- [ ] Clean browser profile, maximized, 100% zoom, bookmarks bar hidden, one tab, no extension badges; OS Do Not Disturb on.
- [ ] Signed in already (credentials never on camera); start URL: `demo.washcentral.com/settings-integrations.html`.
- [ ] Snagit: cursor highlight ON, click animation ON; no on-screen keystroke display needed (this lesson is navigation and reading, no keyboard entry).
- [ ] This is a read-only playbook. Do NOT click **Test Connection** or **Edit Configuration** (step 4, and again on the accounting connector in step 9), and do NOT click **+ New Record** (step 8). **Test Connection is never run and never depicted running** — it is located, ringed, and left alone; no probe result is ever shown or staged.
- [ ] Never read, zoom on, or linger over a masked credential value; the CREDENTIALS block in step 5 is read for field names and empty/Not set states only.
- [ ] Corpus gaps to hold as parked cards, not fabricate: the connected-state status label (step 2), the **Instructions** tab body (step 6), the **Help Desk (Zoho)** page contents (step 7 — the page 404'd on July 9), the **System Users** rows (step 8 — fetch error on July 9), and the accounting connector's field contents (step 9). TBD — parked (see guide Notes).

## Shots

Format: Step, action, expected state, hold, edit notes. "Hold" = seconds of stillness after the expected state renders. Move deliberately: cursor travel about 1.3s, hover about 1s, then read. This is a diagnostic walk; nothing is connected, edited, re-authenticated, or sent.

| Step | Action (exact target) | Expected state | Hold | Edit notes |
|---|---|---|---|---|
| 1 | Open on **Settings → Integrations**; hold while narration scopes the ticket (no click yet) | Integrations page open; on-screen branch caption: "Zoho tickets stopped → Zoho Desk connector · SSO users locked out → SSO provider + System Users SSO column · accounting export gaps → accounting connector (+ GL Codes for mapping)" | 4s | Callout ① a simple three-way branch graphic keyed to steps 2, 8, and 9; no navigation, no connector opened yet |
| 2 | Open **Settings → Integrations → Zoho Desk**; hold on the header | Breadcrumb **Settings › Zoho Desk**; header shows **Zoho Desk**, a **Help Desk Integration** badge, status chip **⚠ Not Configured**, subtitle **Support Tickets · Settings**, type chips **OAuth 2.0 · Live View · Server-side Token** | 4s | Callout ② zoom-in 150% on the **⚠ Not Configured** chip (1.5s); caption "the headline of the whole ticket"; note "connected-state label not in corpus — TBD — parked" |
| 3 | Sweep the four summary tiles left to right | Tiles read **Not set** (Connection), **US** (Data Center), **—** (Org ID), **both** (Sync Direction) | 3s | Callout ③ on **Not set** (Connection); caption "matches the banner: nothing has authenticated"; slow left-to-right sweep |
| 4 | Move the cursor to the top-right pair of buttons; point, do NOT click | **Test Connection** and **Edit Configuration** sit at the top-right of the connector card | 4s | Callout ④ STOP callout with a red "do not click" ring over **Test Connection** ("live probe against the real integration — admin action, never run or depicted from demo") and a second ring over **Edit Configuration** ("credential form — admin only"); cursor points, never presses |
| 5 | On the **Settings** tab, sweep the **CREDENTIALS** block; end on **TOKEN EXPIRY** | Block lists **CLIENT ID** (—), **CLIENT SECRET** (**Not set**), **ORG ID** (—), **DATA CENTER** (us), **ACCESS TOKEN** (**Not set**), **REFRESH TOKEN** (**Not set**), **TOKEN EXPIRY** (—), **ACCOUNTS SERVER** (—) | 5s | Callout ⑤ zoom-in on **TOKEN EXPIRY** and **ACCESS TOKEN** (1.5s); caption "lapsed token = the usual mid-life failure"; STOP reminder "field names only — never a masked value into a ticket or on a zoom" |
| 6 | Point to **GENERAL → ENABLED** and **SYNC → SYNC DIRECTION**, then to the **Instructions** tab (do NOT open it) | **ENABLED** reads **Yes**; **SYNC DIRECTION** reads **both**; the **Instructions** tab sits next to **Settings** | 4s | Callout ⑥ on **ENABLED Yes** ("nobody switched it off — the failure is authentication"); park note on **Instructions**: "reconnect procedure lives here; body not in corpus — TBD — parked"; tab is pointed at, not opened |
| 7 | Navigate **IT & Support → Help Desk (Zoho)**; hold on whatever the page serves | July 9 corpus: `zoho-tickets.html` returned **HTTP Error 404 · Not Found**. Record the live state; if the 404 persists, hold it as the captured state | 4s | Callout ⑦ caption "menu item confirmed (DISCREPANCIES.md #17); page contents TBD — parked"; if the page loads at record time, sweep it slowly and log the drift; NEW-DISCREPANCY tag: plan expects an empty-inbox state, corpus shows a 404 |
| 8 | Navigate **Settings → Users & Security → System Users**; sweep the column headers (do NOT click **+ New Record**) | Panel header **System Users List** with **— Total Users**; search placeholder **Search by name, code, or email…**; columns **USER NAME · USER CODE · USER LEVEL · EMAIL · PHONE · ACTIVE · SSO · LAST LOGIN**. July 9 body read "Couldn't load records (Failed to fetch). Check your connection to the AI agent service." | 5s | Callout ⑧ zoom-in on the **SSO** and **LAST LOGIN** headers (1.5s); caption "SSO down = these users locked out; LAST LOGIN = when each last got in"; STOP callout over **+ New Record**; rows TBD — parked — caption "confirm the fetch before saying no SSO users exist" |
| 9 | Return to **Settings → Integrations**; open the tenant's accounting connector (QuickBooks or Sage, per the plan's PB-13 note); read it the Zoho way, click nothing | Accounting connector page carries the same layout as Zoho: a status chip, **Test Connection**, and a credential and token block. Field contents not captured in this playbook's scope | 4s | Callout ⑨ caption "same read: chip → token → surface"; STOP ring over its **Test Connection** too; field contents TBD — parked — hold the parked card if the page is unavailable; mapping gaps go to GL Codes (Lesson 12.4), not visited here |
| 10 | None — hold (on the accounting connector or back on **System Users**) while narration rules out transient errors | Static screen; on-screen caption: "two screens failed to fetch + one 404 today — an empty list is not proof the integration is down. Reload, then read the connector's own status chip" | 3s | Callout ⑩ the reload-first rule; no navigation required |
| 11 | None — hold while narration packages the escalation | Static screen; on-screen checklist caption: "integration name · status banner quoted (⚠ Not Configured) · Test Connection: run by an admin? result? · failure-window timestamps · NEVER a token, secret, or masked value" | 3s | Callout ⑪ the escalation checklist; STOP reminder "re-auth = Edit Configuration, admin, production — never from demo" |
| 12 | Navigate back to **Settings → Integrations → Zoho Desk**; slow cursor sweep from the status chip down to the credentials block | Recap state: chip → tiles → **TOKEN EXPIRY** → surface checks (Help Desk / **SSO** column / accounting connector) → rule out fetch errors → escalate. Every screen was read-only | 3s | Callout ⑫ tying the flow together; end card / verification stamp "Verified against demo.washcentral.com on July 9, 2026."; fade out |

Estimated runtime: about 4:35 (see narration for the block-by-block total, pending render). If any unexpected dialog, error, changed data, or a "Still there?" timeout prompt appears: stop, note it, dismiss off-camera, resolve or park, then re-record the segment — do not improvise. Steps 7, 8, and 9 record whatever the live tenant serves; where the July 9 corpus showed a 404 or a fetch error, capture the live state and caption the parked contents rather than staging anything.

## If Claude drives while you record

Once the live tenant is reachable I can execute steps 1–12 with the timings above on request; you run Snagit (or any 1920×1080 MP4 recorder) on the window. No action control is ever triggered: **Test Connection** and **Edit Configuration** (on Zoho and on the accounting connector) are read-only holds with do-not-click rings, **+ New Record** stays untouched, and no credential value is ever zoomed, read, or exposed. The Help Desk (Zoho) page, the System Users rows, the Instructions tab body, and the accounting connector's fields are captured live or held as TBD — parked. Say "drive playbook PB-13" when you're rolling.

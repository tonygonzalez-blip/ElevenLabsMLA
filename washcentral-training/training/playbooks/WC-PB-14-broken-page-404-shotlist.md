# WC-PB-14 — Broken page / 404 inside the platform · Snagit Shot List (operator-ready)

v1.0 · July 9, 2026 · Target output: **WC-PB-14-broken-page-404-v1.0.mp4** · MP4 (H.264) · 30 fps · **1920×1080 native, no scaling** · mic and system audio OFF (narration is ElevenLabs only)
Step numbers match the guide and the narration blocks one-to-one (10 steps).
**Verified against demo.washcentral.com on July 9, 2026.**

## Pre-roll checklist (not recorded)

- [ ] Display/capture region exactly 1920×1080; verify the first export's pixel dimensions before recording anything else.
- [ ] Clean browser profile, maximized, 100% zoom, bookmarks bar hidden, one tab, no extension badges; OS Do Not Disturb on. The browser's URL bar must stay visible in frame — this lesson reads it on camera.
- [ ] Signed in already (credentials never on camera); start URL: `demo.washcentral.com/settings.html`.
- [ ] Snagit: cursor highlight ON, click animation ON; no on-screen keystroke display needed (this lesson is navigation and reading, no keyboard entry).
- [ ] This is a read-and-classify playbook. Do NOT click **+ New Record** (steps 5, 9), **Create ticket** (step 9), or **Export** (step 9). No ticket is submitted on the shared demo. The only "safe" click is the **Fundraiser** stub in step 4, which by design changes nothing.
- [ ] **Title-card rule (this lesson only):** the 404, stub, and data-error classes may be shown as live captures, as the committed July 8/9 screenshots, or as comparison/checklist cards, per the guide. Never stage or fake a failure state; if a page that 404'd on July 9 loads today, record what it serves and log the drift.
- [ ] LogicPOS (step 2) was NOT re-crawled on July 9; its 404 status is parked (PARKED-ITEMS.md #2, last confirmed July 8). Show it as the attributed July 8 capture or a parked card — do not navigate blind and improvise.

## Shots

Format: Step, action, expected state, hold, edit notes. "Hold" = seconds of stillness after the expected state renders. Move deliberately: cursor travel about 1.3s, hover about 1s, then read. Nothing here changes a record.

| Step | Action (exact target) | Expected state | Hold | Edit notes |
|---|---|---|---|---|
| 1 | Open on the **Settings** hub; zoom the browser URL bar, then hold on a title card naming the two reads | Settings hub open; on-screen caption: "Reproduce at the exact address. Then read two things: the URL in the bar, and the message on the page." | 4s | Callout ① title card with the two reads; zoom-in 150% on the URL bar (1.5s); caption "three failures hide behind 'the page is broken'" |
| 2 | Class one, hard 404: point to the **LogicPOS** links advertised on the Settings hub (`logicpos/sales.html`, `logicpos/login.html`, `logicpos/dashboard.html`); then show the July 8 captured error as a screen or card | On July 8, 2026 all three returned the IIS page "404 – File or directory not found": no WashCentral header, no sidebar, the server's own error page at the requested address | 4s | Callout ② on the three link targets; ATTRIBUTED-SCREEN tag "July 8 capture — not re-crawled July 9; status today TBD — parked (PARKED-ITEMS.md #2)"; caption "hard 404 = the server has no file there" |
| 3 | Open **IT & Support → System Alerts**; hold on whatever it serves (July 9: an in-app 404) | July 9 capture: "HTTP Error 404. The requested resource is not found." while **System Alerts** remains listed in the **IT & Support** sidebar | 4s | Callout ③ split emphasis: the sidebar entry AND the 404 body in one frame; caption "a listed feature can still 404 — release-gated or mid-deploy, never 'it doesn't exist'"; contents of the working page TBD — parked; if it loads today, record it and log the drift |
| 4 | Class two, the dead stub: open **CRM**, point to **Fundraiser** in the sidebar, click it once, then zoom the URL bar | **Fundraiser** appears in the **CRM** sidebar; the click changes nothing on screen and the address only gains a trailing "#" | 4s | Callout ④ zoom-in 150% on the URL bar showing the trailing "#" (1.5s); caption "menu item with no page wired behind it — not a 404, not the connection" (PARKED-ITEMS.md #3, DISCREPANCIES.md #5); this is the lesson's only click on a failure surface, safe by design |
| 5 | Class three, the data error: open **Settings → Hardware → Terminals**; hold on the load state (do NOT click **+ New Record**) | The page itself loads: breadcrumb, the **Total Terminals** KPI, **+ New Record**, and the column headers all render; where the rows belong it reads "Couldn't load records (Failed to fetch). Check your connection to the AI agent service." | 4s | Callout ⑤ bracketing the fetch-error banner; STOP callout with a red "do not click" ring over **+ New Record**; caption "class three: the page loaded, one data call didn't" |
| 6 | Stay on **Terminals**; slow cursor sweep over everything that DID render | Header, sidebar, KPI band, and column headers all present; only the row data is missing | 4s | Callout ⑥ tracing the intact shell, then the empty row area; caption "usually transient — refresh first; ~60 pages showed this banner in the July 9 sweep = one service issue, not sixty broken pages" |
| 7 | None — hold on a three-column comparison card while narration sorts the classes | Static comparison card: "whole page replaced + URL as requested → hard **404** · click does nothing + URL gains '#' → dead **#** stub · page frame loads + 'Couldn't load records (Failed to fetch)' panel → data-fetch error" | 4s | Callout ⑦ the three-way card keyed to steps 2–3, 4, and 5–6; title-card shot, no navigation |
| 8 | None — hold on an evidence checklist card | Static checklist card: "exact URL · screenshot of the message · module · link source (where you clicked from) · expected target" plus per-class extras: "404 → known parked item? (LogicPOS = #2) · stub → note it (Fundraiser = #3) · fetch error → does refresh clear it? how many pages show it?" | 4s | Callout ⑧ the five core facts, then the per-class extras; title-card shot, no navigation |
| 9 | Open **IT & Support → Support Tickets**; sweep the list columns, then point to the **Support Queue** view's controls (click nothing) | List columns **Ticket # · Subject · Location · Priority · Status · Assigned To · Created**; the **Support Queue** view offers **Create ticket** and sorts by **Ticket · Category · Location · Owner · Age · Priority** | 5s | Callout ⑨ on the seven list columns; STOP callouts with red rings over **+ New Record**, **Create ticket**, and **Export** ("real escalations only, on your tenant — never submitted from the shared demo"); caption "transient fetch error that clears on refresh = no ticket; migration switches = Jira, not here" |
| 10 | Navigate back to the **Settings** hub (or hold on a recap card) | Recap state: read the URL + the message → hard **404** (serving problem) / dead **#** stub (release-gated, never "gone") / **Failed to fetch** (retry first, escalate only if it persists) → ticket with the five facts + the class. Nothing was changed | 3s | Callout ⑩ tying the three classes together; end card / verification stamp "Verified against demo.washcentral.com on July 9, 2026."; fade out |

Estimated runtime: about 3:55 (see narration for the block-by-block total, pending render). If any unexpected dialog, error, changed data, or a "Still there?" timeout prompt appears: stop, note it, dismiss off-camera, resolve or park, then re-record the segment — do not improvise. This lesson's failure states are shown as they were captured (July 8 for LogicPOS, July 9 for System Alerts and Terminals) or as live states with drift logged; steps 7 and 8 are pure title cards. Never fake a 404, a stub, or a fetch error.

## If Claude drives while you record

Once the live tenant is reachable I can execute steps 1–10 with the timings above on request; you run Snagit (or any 1920×1080 MP4 recorder) on the window. The only click on a failure surface is the **Fundraiser** stub, which changes nothing by design; **+ New Record**, **Create ticket**, and **Export** are read-only holds, no ticket is submitted, and any page whose July 9 state has drifted is recorded as it serves today with the drift logged, or held as the attributed capture / parked card. Say "drive playbook PB-14" when you're rolling.

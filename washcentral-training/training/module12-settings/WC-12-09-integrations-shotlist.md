# WC-12-09 — Integrations (SSO, Accounting, Help Desk) · Snagit Shot List (operator-ready)

v1.0 · July 9, 2026 · Target output: **WC-12-09-integrations-v1.0.mp4** · MP4 (H.264) · 30 fps · **1920×1080 native, no scaling** · mic and system audio OFF (narration is ElevenLabs only)
Step numbers match the guide and the narration blocks one-to-one (14 steps).
**Verified against demo.washcentral.com on July 9, 2026.**

## Pre-roll checklist (not recorded)

- [ ] Display/capture region exactly 1920×1080; verify the first export's pixel dimensions before recording anything else.
- [ ] Clean browser profile, maximized, 100% zoom, bookmarks bar hidden, one tab, no extension badges; OS Do Not Disturb on.
- [ ] Signed in already (credentials never on camera); start URL: the **Zoho Desk** integration page under `Settings → Integrations`.
- [ ] The header shows **Zoho Desk**, the **Help Desk Integration** badge, and the **⚠ Not Configured** pill before rolling; the **Settings** tab is the active tab.
- [ ] This lesson is view only. Do NOT click **Test Connection** or **Edit Configuration** on any page. Do NOT enter or edit any credential field. Do NOT click **Copy** on the redirect/URL fields.
- [ ] If a "Still there?" session-timeout dialog appears, dismiss it off-camera and re-roll the segment (session timeout is out of scope here; see WC-01-03 / PARKED-ITEMS.md #9).

## Shots

Format: Step, action, expected state, hold, edit notes. "Hold" = seconds of stillness after the expected state renders. Move deliberately: cursor travel about 1.3s, hover about 1s, then read. Every page in this lesson is a static settings page; nothing on it should change while you record.

| Step | Action (exact target) | Expected state | Hold | Edit notes |
|---|---|---|---|---|
| 1 | Open on the **Zoho Desk** page; slowly sweep the cursor from the left rail **Integrations** entry up to the breadcrumb | Breadcrumb reads `Settings › Zoho Desk`; left rail shows **Integrations** highlighted at the bottom | 3s | Callout ① bracketing the breadcrumb + the **Integrations** rail entry; slow diagonal cursor move (about 2s), no click |
| 2 | Point to the header identity block (title, badge, status pill, subtitle) | Title **Zoho Desk** beside a **Help Desk Integration** badge and a **⚠ Not Configured** pill; subtitle **Support Tickets · Settings** | 3s | Callout ② on the badge + pill; zoom-in 150% on the **⚠ Not Configured** pill (1.5s) |
| 3 | Point to the three method chips under the title | Chips read **OAuth 2.0**, **Live View**, **Server-side Token** | 3s | Callout ③ across the three chips; slow left-to-right cursor sweep (about 1.5s); do not click a chip |
| 4 | Point to the top-right buttons **Test Connection** and **Edit Configuration** — do NOT click either | Both buttons visible; nothing happens (no click) | 3s | Callout ④ on the two buttons; **STOP** callout with a red "do not click" ring covering **Test Connection** AND **Edit Configuration**; hover near but never on them; neither is clicked |
| 5 | Sweep the cursor across the four status tiles | Tiles read **Connection** (Not set), **Data Center** (US), **Org ID** (—), **Sync Direction** (both) | 4s | Callout ⑤ under the tile row; note **Connection** = Not set agrees with the **⚠ Not Configured** pill; slow sweep left-to-right (about 2.5s) |
| 6 | Point to the **Settings** and **Instructions** tabs (leave **Settings** active) | Two tabs; **Settings** is the open tab, **Instructions** is inactive | 2s | Callout ⑥ on the tab pair; do NOT click **Instructions** (its content is parked); brief zoom on the active-tab underline (1s) |
| 7 | Slowly scroll to and sweep the **CREDENTIALS** group (labels only) | Group **CREDENTIALS** shows **CLIENT ID**, **CLIENT SECRET**, **ORG ID**, **DATA CENTER**, **ACCESS TOKEN**, **REFRESH TOKEN**, **TOKEN EXPIRY**, **ACCOUNTS SERVER**; secret fields sit empty (Not set / —) | 4s | Callout ⑦ "read labels, never values"; slow vertical sweep down the group (about 3s); do not click into any field; fields are empty so nothing masked |
| 8 | Continue scrolling; sweep **GENERAL**, **ROUTING**, **SYNC**, **BEHAVIOR**, **MAPPING** | **GENERAL** (**ENABLED**, **ADMIN NOTES**), **ROUTING** (**DEFAULT DEPARTMENT**, **DEFAULT ASSIGNEE**), **SYNC** (**SYNC DIRECTION** both), **BEHAVIOR** (**AUTO-CREATE CONTACTS**), **MAPPING** (**STATUS MAPPING**, **SLA MAPPING**) | 4s | Callout ⑧ on the group headers; slow scroll-and-hold on each header (about 3s); end back near the top of the page |
| 9 | Navigate to the **Microsoft Entra ID** integration page | Same skeleton: **SSO Integration** badge, **⚠ Not Configured** pill, subtitle **Single Sign-On · Settings**, chips **Authentication / Microsoft 365 / MSAL.js**; tiles **Connection · Issuer · Client ID · Scopes**; a **REDIRECT URI** field with a **Copy** button | 4s | Callout ⑨ on the badge + chips; second small callout on the **REDIRECT URI** + **Copy**; do NOT click **Copy**; zoom on the tile row (1.5s) |
| 10 | Navigate through **Google Workspace SSO**, then **Okta**, then **Amazon Cognito**; hold on each header | Identical layout each time; chips differ: Google **Authentication / Google Workspace / OAuth 2.0**, Okta **Authentication / OIDC / PKCE**, Cognito **Authentication / AWS / PKCE / OAuth 2.0**; same four tiles, same **⚠ Not Configured** | 5s (about 1.6s per page) | Callout ⑩ tracking "only the name + chips change"; quick cut between the three headers; zoom on each chip row (1s each); do NOT click **Test Connection** or **Copy** on any page |
| 11 | Navigate to **SCIM Provisioning** | **User Provisioning** badge, subtitle **SCIM 2.0 · Settings**, chips **Auto-Provisioning / Microsoft Entra ID / REST API**, a **← SSO Settings** back link; tiles **Status** (Not Set Up), **SCIM URL**, **Bearer Token** (Not Set), **Last Event**; groups **ENDPOINT CONFIGURATION** (**FULL TENANT URL** + **Copy**, **BEARER TOKEN**) and an empty **SYNC LOG** | 4s | Callout ⑪ on the tiles + **SYNC LOG** empty state; do NOT click **Copy**, **← SSO Settings**, **Test Connection**, or **Edit Configuration**; zoom on the **Status** tile (1.5s) |
| 12 | Navigate to **Sage Accounting** | **Accounting Integration** badge, subtitle **General Ledger · Settings**, chips **Accounting / ERP / XML API**; tiles **Connection · Environment** (Production) **· Company ID · Sync Schedule** (0 2 * * *); groups include **SYNC** (**SYNC SCHEDULE (CRON)**, **BATCH SIZE**, **MAX RETRIES**) and **SAFETY** (**DRY RUN**) | 4s | Callout ⑫ on the **Sync Schedule** cron tile; slow sweep to the **SAFETY / DRY RUN** group (about 2s); do NOT click **Test Connection** |
| 13 | Navigate to **QuickBooks Online** | **Accounting Integration** badge, subtitle **General Ledger · Settings**, chips **Accounting / OAuth 2.0 / REST API**; tiles **Connection · Environment · Realm · Token**; **CREDENTIALS** group names **REALM / COMPANY ID** with the OAuth tokens | 4s | Callout ⑬ contrasting QuickBooks vs Sage (same job, different vendor); zoom on the chip row (1.5s); do NOT click **Test Connection** |
| 14 | Return to the **Zoho Desk** page; sweep the status pill, then the **Test Connection** button, then the **CREDENTIALS / TOKEN EXPIRY** field, in that order | Recap state: the three-step read — **⚠ Not Configured** status → **Test Connection** (admin runs it) → **TOKEN EXPIRY** / credential state; answerable without filing a bug | 4s | Callout ⑭ numbering the three read points ①②③; end card / verification stamp "Verified against demo.washcentral.com on July 9, 2026."; **Test Connection** is never clicked; fade out |

Estimated runtime: about 5:50 including the extra cursor travel across eight integration pages; the narration bed underneath is estimated ~5:22 and the block timestamps in the narration script are estimates until the audio is generated. If any unexpected dialog, error, changed data, or a "Still there?" timeout prompt appears: stop, note it, dismiss off-camera, resolve or park, then re-record the segment (do not improvise). Session-timeout behavior is out of scope here (see WC-01-03).

## If Claude drives while you record

I can execute steps 1–14 live in the browser with the timings above on request; you run Snagit (or any 1920×1080 MP4 screen recorder) on the window. This lesson is view only from end to end: **Test Connection** and **Edit Configuration** are located but never clicked, **Copy** on the redirect/URL fields is never clicked, and no credential field is ever entered or edited. Say "drive lesson 12-09" when you're rolling.
